## 졸업 선물

### 1. 문제 정보

- **문제 제목**: 졸업 선물
- **카테고리**: 완전탐색

### 2. 문제 설명

- **핵심 목표**: 한정된 예산 M 내에서 최대한 많은 학생에게 선물을 사준다.
- **제한 사항**:
  - 학생 수 N (최대 1,000), 예산 M (최대 1억)
  - 쿠폰 하나: 딱 하나의 상품 가격만 50% 할인 가능 (배송비 제외)
- **입출력 예시**:
  - Input: 예산 28, (가격, 배송비) 리스트
  - Output: 4 (어떤 상품을 할인받고 나머지 저렴한 순으로 구매 시 4명 가능)

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  1. 전수 조사
- **풀이 로직**:
  1. 정렬: (가격 + 배송비) 합계가 낮은 순서대로 정렬한다. (그리디적 접근)
  2. 할인 대상 선정: 0번부터 n-1번 학생까지 한 번씩 할인 쿠폰을 적용한다고 가정한다.
  3. 예산 소진: 할인 적용 후 남은 예산으로 나머지 학생들의 선물을 저렴한 순서대로 구매한다.
  4. 비교: 각 케이스마다 구매 가능한 학생 수를 Math.max로 비교하여 최적의 해를 도출한다.

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도**                              | **공간 복잡도** |
| -------------- | -------------------- | -------------------------------------------- | --------------- |
| **Solution 1** | **0.1501ms**         | O(N²) (정렬 O(N log N) + 이중 반복문 O(N^2)) | O(N)            |

---

## 💻 JavaScript 풀이 코드

Solution 1: 각 상품을 하나씩 할인 대상으로 지정해 보며 가격이 낮은 순서대로 추가하며 최대 인원 찾기

```js
function solution1(m, product) {
  let answer = 0;
  const n = product.length;

  // 1. 총비용(가격+배송비) 기준 정렬
  product.sort((a, b) => a[0] + a[1] - (b[0] + b[1]));

  for (let i = 0; i < n; i++) {
    // 2. i번째 학생 상품 50% 할인 적용 후 남은 예산 계산
    let money = m - product[i][0] * 0.5 - product[i][1];
    let count = 1;

    for (let j = 0; j < n; j++) {
      if (i === j) continue; // 이미 할인해서 산 상품은 건너뜀
      const price = product[j][0] + product[j][1];

      // 3. 남은 예산으로 구매 가능한지 확인
      if (money - price < 0) break;
      money -= price;
      count++;
    }

    answer = Math.max(answer, count);
  }

  return answer;
}
```
