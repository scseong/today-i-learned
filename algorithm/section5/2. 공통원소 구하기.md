## 공통원소 구하기

### 1. 문제 정보

- **문제 제목**: 공통원소 구하기
- **카테고리**: 효율성, 두 포인터

### 2. 문제 설명

- **핵심 목표**: : 두 집합 A, B에서 공통으로 존재하는 원소를 찾아 오름차순으로 정렬하여 출력
- **제한 사항**:
  - 집합의 크기 N, M <= 30,000
  - 원소의 크기 <= 1,000,000,000
- **입출력 예시**:
  - Input: [1, 3, 9, 5, 2], [3, 2, 5, 7, 8]
  - Output: [2, 3, 5]

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  1. 정렬 후 두 포인터 비교
- **풀이 로직**:
  1. 두 배열 arr1, arr2를 오름차순으로 정렬
  2. 두 포인터 탐색
  - arr1[p1] === arr2[p2]: 공통 원소이므로 answer에 추가하고 두 포인터 모두 증가
  - arr1[p1] < arr2[p2]: arr1의 현재 값이 더 작으므로 p1을 증가
  - arr1[p1] > arr2[p2]: arr2의 현재 값이 더 작으므로 p2를 증가

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도**      | **공간 복잡도** |
| -------------- | -------------------- | -------------------- | --------------- |
| **Solution 1** | **0.0926m**          | O(N log N + M log M) | O(min(N, M))    |

---

## 💻 JavaScript 풀이 코드

Solution 1: 두 배열을 오름차순으로 정렬한 뒤, 두 포인터를 비교하며 공통 원소를 추출

```js
function solution1(arr1, arr2) {
  let answer = [];
  let [p1, p2] = [0, 0];

  // 1. 오름차순 정렬
  arr1.sort((a, b) => a - b);
  arr2.sort((a, b) => a - b);

  const n = arr1.length;
  const m = arr2.length;

  // 2. 두 포인터가 끝에 도달할 때까지 비교
  while (p1 < n && p2 < m) {
    if (arr1[p1] === arr2[p2]) {
      // 공통 원소 발견
      answer.push(arr1[p1]);
      p1++;
      p2++;
    } else if (arr1[p1] > arr2[p2]) {
      // arr2의 값이 더 작으므로 p2 이동
      p2++;
    } else {
      // arr1의 값이 더 작으므로 p1 이동
      p1++;
    }
  }

  return answer;
}
```
