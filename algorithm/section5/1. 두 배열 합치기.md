## 두 배열 합치기

### 1. 문제 정보

- **문제 제목**: 두 배열 합치기
- **카테고리**: 효율성, 두 포인터 (Two Pointers)

### 2. 문제 설명

- **핵심 목표**: 이미 오름차순 정렬된 두 배열을 합쳐서 하나의 정렬된 배열로 만든다.
- **제한 사항**:
  - 배열 N, M의 크기 (최대 100)
  - 반드시 O(N+M)의 시간 복잡도
- **입출력 예시**:
  - Input: [1, 3, 5], [2, 3, 6, 7, 9]
  - Output: [1, 2, 3, 3, 5, 6, 7, 9]

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  1. 두 포인터 (Two Pointers)
  2. 정렬
- **풀이 로직**:
  1. 두 포인터 (Two Pointers)
  - 각 배열의 인덱스를 가리키는 포인터 p, q를 활용
  - 두 요소를 비교해 작은 값을 결과에 넣고 해당 포인터를 이동
  2. 전개 연산자로 합친 후 내림차순 정렬

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도** | **공간 복잡도** |
| -------------- | -------------------- | --------------- | --------------- |
| **Solution 1** | **0.0975ms**         | O(N+M)          | O(N+M)          |
| **Solution 2** | **0.0820ms**         | O(N log M)      | O(N+M)          |

---

## 💻 JavaScript 풀이 코드

Solution 1: 두 포인터 사용

```js
function solution1(arr1, arr2) {
  let answer = [];
  let [p, q] = [0, 0];
  const n = arr1.length;
  const m = arr2.length;

  // 두 포인터가 모두 유효한 범위 내에 있을 때 비교
  while (p < n && q < m) {
    if (arr1[p] <= arr2[q]) answer.push(arr1[p++]);
    else answer.push(arr2[q++]);
  }

  // 남은 요소가 있는 배열의 나머지 부분을 합치기
  while (p < n) answer.push(arr1[p++]);
  while (q < m) answer.push(arr2[q++]);

  return answer;
}
```

Solution 2: 내장 메서드 사용

```js
function solution2(arr1, arr2) {
  return [...arr1, ...arr2].sort((a, b) => a - b);
}
```
