## 학급 회장 (해쉬)

### 1. 문제 정보

- **문제 제목**: 학급 회장 (해쉬)
- **카테고리**: 효율성, 해쉬 (Hash), Map 자료구조

### 2. 문제 설명

- **핵심 목표**: 주어진 투표 결과 문자열에서 가장 많이 등장한 알파벳(후보 기호)을 찾는다.
- **제한 사항**:
  - 학생 수 N (5 ≤ N ≤ 50)
  - 반드시 한 명의 당선자가 나오는 입력만 주어진다.
- **입출력 예시**:
  - Input: BACBACCACCBDEDE
  - Output: C

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  1. 해쉬 맵
- **풀이 로직**:
  1. 각 후보의 기호를 키(Key)로, 득표수를 값(Value)으로 저장할 Map 객체 생성
  2. 문자열을 순회하며 현재 문자가 Map에 있으면 기존 값에 +1, 없으면 생성
  3. Map에 저장된 데이터를 순회하며 가장 높은 값을 가진 키를 반환

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도** | **공간 복잡도** |
| -------------- | -------------------- | --------------- | --------------- |
| **Solution 1** | **0.0770ms**         | O(N)            | O(M)            |

---

## 💻 JavaScript 풀이 코드

Solution 1: Map 객체를 생성하여 문자열의 각 기호를 카운팅하고, 가장 높은 득표수를 가진 키를 반환

```js
function solution(s) {
  let answer;
  let sH = new Map();

  // 1. 투표 결과 카운팅
  for (let x of s) {
    sH.set(x, (sH.get(x) || 0) + 1);
  }

  // 2. 최대 득표자 찾기
  let max = -1;
  for (let [key, val] of sH) {
    if (val > max) {
      max = val;
      answer = key;
    }
  }

  return answer;
}
```
