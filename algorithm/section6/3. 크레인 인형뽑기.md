## 크레인 인형뽑기 (카카오 기출)

### 1. 문제 정보

- **문제 제목**: 크레인 인형뽑기 (카카오 기출)
- **카테고리**: 자료구조, 스택(Stack), 2차원 배열 탐색

### 2. 문제 설명

- **핵심 목표**: 2차원 격자판에서 인형을 뽑아 스택(바구니)에 쌓고, 연속된 같은 인형 두 개가 터진 총 개수를 구한다.
- **제한 사항**:
  - board 크기: 5x5 ~ 30x30
  - moves 길이: 1 ~ 1,000
- **입출력 예시**:
  - Input: [
    [0, 0, 0, 0, 0],
    [0, 0, 1, 0, 3],
    [0, 2, 5, 0, 1],
    [4, 2, 4, 4, 2],
    [3, 5, 1, 3, 1],
    ]
  - Output: 4

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  1. 스택(Stack) 자료구조
- **풀이 로직**:
  - moves를 순회하며 각 크레인 위치(pos - 1)를 확인
  - 해당 열의 0행부터 마지막 행까지 탐색하여 0이 아닌 인형을 찾기
  - 인형을 찾으면
    - 스택의 가장 위(stack.length - 1)와 현재 인형이 같은지 비교
    - 같으면 pop()하고 answer += 2
    - 다르면 push()
  - 인형을 뽑은 자리는 0으로 만들고 break

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도** | **공간 복잡도** |
| -------------- | -------------------- | --------------- | --------------- |
| **Solution 1** | **0.118ms**          | O(M \* N)       | O(M)            |

---

## 💻 JavaScript 풀이 코드

Solution 1: 2차원 배열 열 탐색과 스택 활용

```js
function solution1(board, moves) {
  let answer = 0;
  let stack = [];

  for (let pos of moves) {
    // 1. 해당 열을 위에서부터 탐색
    for (let i = 0; i < board.length; i++) {
      if (board[i][pos - 1] !== 0) {
        let doll = board[i][pos - 1];
        board[i][pos - 1] = 0; // 뽑은 자리 빈칸 처리

        // 2. 스택 상단과 비교하여 터뜨리기 로직
        if (doll === stack[stack.length - 1]) {
          stack.pop();
          answer += 2;
        } else {
          stack.push(doll);
        }

        break;
      }
    }
  }

  return answer;
}
```
