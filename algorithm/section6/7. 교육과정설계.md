## 교육과정설계

### 1. 문제 정보

- **문제 제목**: 교육과정설계
- **카테고리**: 자료구조, 큐(Queue)

### 2. 문제 설명

- **핵심 목표**: 필수 과목의 순서가 주어질 때 수업 설계가 그 순서를 유지하며 모든 필수 과목을 포함하고 있는지 판별한다.
- **제한 사항**:
  - 필수 과목 순서
  - 수업 설계 문자열 (길이 30 이하)
  - 올바르면 "YES", 아니면 "NO" 출력
- **입출력 예시**:
  - Input: CBA, CBDAGE
  - Output: YES

### 3. 접근 방식 및 알고리즘

- **사용한 알고리즘**:
  - 큐(Queue)
- **풀이 로직**:
  - 필수 과목 문자열을 분리하여 큐(Queue)에 순서대로 삽입
  - 수업 설계(plan)의 각 과목을 하나씩 확인
  - 현재 확인 중인 과목이 필수 과목 큐에 존재하는지 확인
    - 필수 과목이라면 큐의 맨 앞(shift)에서 꺼낸 과목과 일치해야 함
    - 만약 일치하지 않는다면 순서가 어긋난 것이므로 즉시 "NO"를 반환
  - 큐에 남은 과목이 있다면 필수 과목을 누락한 것이므로 "NO"를 반환

### 4. 성능 측정 결과 (Performance)

| **구분**       | **실행 시간 (평균)** | **시간 복잡도** | **공간 복잡도** |
| -------------- | -------------------- | --------------- | --------------- |
| **Solution 1** | **0.0466ms**         | O(N \* K)       | O(K)            |

---

## 💻 JavaScript 풀이 코드

Solution 1: Queue를 활용한 순차 검증

```js
function solution(need, plan) {
  let answer = "YES";
  let queue = need.split("");

  for (let x of plan) {
    if (queue.includes(x)) {
      if (x !== queue.shift()) return "NO";
    }
  }

  if (queue.length > 0) return "NO";

  return answer;
}
```
